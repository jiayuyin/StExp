常用的命令：
```
svn co(checkout) [-r revision] xxxxxxxxxxxxxx --username xx --password xx
svn ci(commit) xxx xxx -F log.txt --username xx
svn copy svnxxxxxxxxxx ./
svn move xx xxx
svn propset svn:executable on xxx //设置执行权限
svn add
svn delete
```

